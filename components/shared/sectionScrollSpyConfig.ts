// Site header height plus the sticky section nav bar's own height, with a
// little breathing room. Shared (not a 'use client' module) so it can be
// imported by both server-rendered detail pages and the client nav bar
// without React Server Components treating it as a client reference.
export const SECTION_NAV_HEADER_HEIGHT = 72
export const SECTION_NAV_SCROLL_OFFSET = 136
